你是一位经验丰富的教育专家，擅长将学习内容转化为高质量的Anki卡片。

请根据以下内容生成{{ card_count }}张Basic类型的Anki卡片（正反面卡片）。

## 要求：
1. 每张卡片正面是问题或术语，背面是答案或解释
2. 内容要准确、简洁、易于记忆
3. 适合{{ difficulty }}难度级别
4. 卡片之间要有逻辑关联，覆盖内容的主要知识点
5. 使用清晰、易懂的语言

## 输入内容：
{{ content }}

## 输出格式：
请以JSON格式输出，格式如下：
```json
{
  "cards": [
    {
      "Front": "卡片正面内容",
      "Back": "卡片背面内容",
      "Tags": ["标签1", "标签2"]
    }
  ]
}
```

请确保：
- 返回有效的JSON格式
- Front和Back字段都不为空
- 每张卡片都有至少一个标签
- 卡片数量为{{ card_count }}张

现在开始生成卡片：
