你是一位经验丰富的教育专家，擅长将学习内容转化为高质量的Anki填空卡片。

请根据以下内容生成{{ card_count }}张Cloze类型的Anki卡片（填空卡片）。

## 要求：
1. 每张卡片使用{% raw %}{{c1::答案}}{% endraw %}格式标记需要填空的关键词
2. 一张卡片可以有多个填空（使用{% raw %}{{c1::}}{% endraw %}, {% raw %}{{c2::}}{% endraw %}, {% raw %}{{c3::}}{% endraw %}等）
3. 填空应该是关键概念、术语或重要信息
4. 内容要准确、简洁、易于记忆
5. 适合{{ difficulty }}难度级别
6. 卡片之间要有逻辑关联，覆盖内容的主要知识点
{% if basic_tags or optional_tags %}
7. 标签要求：
   {% if basic_tags %}
   - 所有卡片必须包含以下基础标签：{{ basic_tags | join(', ') }}
   {% endif %}
   {% if optional_tags %}
   - 可选标签列表（请根据卡片内容选择合适的标签）：
     {% for tag in optional_tags %}
     - {{ tag }}
     {% endfor %}
   - 每张卡片应选择1-3个最相关的可选标签，与基础标签组合使用
   {% endif %}
   - 标签必须从上述列表中选择，不要随意创建新标签
{% endif %}

## 输入内容：
{{ content }}

## 输出格式：
请以JSON格式输出，格式如下：
```json
{
  "cards": [
    {
      "Text": "包含{% raw %}{{c1::填空}}{% endraw %}的完整句子或段落",
      "Tags": ["标签1", "标签2"]
    }
  ]
}
```

## 示例：
正面：Python是一种{% raw %}{{c1::高级编程语言}}{% endraw %}，由{% raw %}{{c2::Guido van Rossum}}{% endraw %}在1991年发布。
背面：Python是一种{% raw %}{{c1::高级编程语言}}{% endraw %}，由{% raw %}{{c2::Guido van Rossum}}{% endraw %}在1991年发布。

请确保：
- 返回有效的JSON格式
- Text字段包含至少一个{% raw %}{{cN::}}{% endraw %}标记
- 每张卡片都有至少一个标签
- 卡片数量为{{ card_count }}张

现在开始生成卡片：
