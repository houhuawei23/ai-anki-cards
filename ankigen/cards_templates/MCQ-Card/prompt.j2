你是一位经验丰富的教育专家，擅长将学习内容转化为高质量的多项选择题。

请根据以下内容生成{{ card_count }}张MCQ类型的Anki卡片（多项选择题）。

## 要求：
1. 每张卡片包含一个问题（Question）和多个选项（OptionA, OptionB, OptionC等，最多6个选项）
2. 支持单选题和多选题：
   - 单选题：Answer字段填写单个大写字母（如 "A"）
   - 多选题：Answer字段填写多个大写字母（如 "AC" 或 "ABC"）
3. 干扰项要合理，不能过于明显错误
4. 选项要简洁明了，长度相近
5. 内容要准确、有教育价值
6. 适合{{ difficulty }}难度级别
7. 卡片之间要有逻辑关联，覆盖内容的主要知识点
8. Note字段提供题目整体的解释说明
9. NoteA-F字段（可选）提供对应选项的详细说明，未使用的选项可以不填写
{% if basic_tags or optional_tags %}
10. 标签要求：
   {% if basic_tags %}
   - 所有卡片必须包含以下基础标签：{{ basic_tags | join(', ') }}
   {% endif %}
   {% if optional_tags %}
   - 可选标签列表（请根据卡片内容选择合适的标签）：
     {% for tag in optional_tags %}
     - {{ tag }}
     {% endfor %}
   - 每张卡片应选择1-3个最相关的可选标签，与基础标签组合使用
   {% endif %}
   - 标签必须从上述列表中选择，不要随意创建新标签
{% endif %}

## 输入内容：
{{ content }}

## 输出格式：
请以JSON格式输出，格式如下：
```json
{
  "cards": [
    {
      "Question": "问题内容（支持HTML格式，如加粗、公式等）",
      "OptionA": "选项A的内容",
      "OptionB": "选项B的内容",
      "OptionC": "选项C的内容",
      "OptionD": "选项D的内容",
      "Answer": "AC",
      "Note": "题目的整体解释说明。对于单选题，请写单个大写字母，如A。对于多选题，请写所有正确答案的字母，如ABC。",
      "NoteA": "选项A的详细说明（可选）",
      "NoteB": "选项B的详细说明（可选）",
      "NoteC": "选项C的详细说明（可选）",
      "Tags": ["标签1", "标签2"]
    }
  ]
}
```

## 示例：
```json
{
  "cards": [
    {
      "Question": "以下哪些是Python的特点？",
      "OptionA": "解释型语言",
      "OptionB": "编译型语言",
      "OptionC": "动态类型",
      "OptionD": "静态类型",
      "Answer": "AC",
      "Note": "Python是解释型语言，代码在运行时由解释器逐行执行。Python也是动态类型语言，变量类型在运行时确定。",
      "NoteA": "Python是解释型语言，不需要预先编译成机器码",
      "NoteC": "Python是动态类型语言，变量类型在运行时确定",
      "Tags": ["编程", "Python"]
    }
  ]
}
```

请确保：
- 返回有效的JSON格式
- Question字段不为空
- 至少包含OptionA和OptionB两个选项，最多6个选项（OptionA-F）
- 未使用的选项字段（如OptionE、OptionF）可以不填写或留空
- Answer字段填写正确答案的大写字母（单选题如"A"，多选题如"AC"或"ABC"）
- Note字段提供题目的整体解释说明
- NoteA-F字段（可选）提供对应选项的详细说明，未使用的可以不填写
- 每张卡片都有至少一个标签
- 卡片数量为{{ card_count }}张

现在开始生成卡片：
